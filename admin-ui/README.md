# 🎵 音乐管理平台 - 前端

基于 Vue 3 + Ant Design Vue 4 + TypeScript 的音乐管理平台前端。

## 技术栈

- **框架**: Vue 3.4 + TypeScript
- **UI组件库**: Ant Design Vue 4.x
- **构建工具**: Vite 5
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP客户端**: Axios
- **CSS方案**: UnoCSS

## 功能模块

- **仪表盘**: 数据统计、最近添加的音乐
- **音乐搜索**: 搜索酷我音乐、一键解析保存
- **音乐管理**: 列表展示、筛选、播放、删除

## 快速开始

### 1. 安装依赖

```bash
cd admin
pnpm install
```

### 2. 启动开发服务

```bash
pnpm dev
```

访问 http://localhost:5173

### 3. 构建生产版本

```bash
pnpm build
```

## 目录结构

```
admin/
├── public/                 # 静态资源
├── src/
│   ├── api/                # API 接口
│   ├── assets/             # 资源文件
│   ├── components/         # 公共组件
│   ├── composables/        # 组合式函数
│   ├── layouts/            # 布局组件
│   ├── pages/              # 页面组件
│   ├── router/             # 路由配置
│   ├── stores/             # Pinia 状态
│   ├── utils/              # 工具函数
│   ├── App.vue
│   └── main.ts
├── types/                  # 类型定义
├── .env                    # 环境变量
├── index.html
├── package.json
├── tsconfig.json
├── unocss.config.ts
└── vite.config.ts
```

## 环境变量

```env
VITE_APP_BASE_API=/api      # API 基础路径
VITE_APP_TITLE=音乐管理平台   # 应用标题
```

## 开发代理

开发环境已配置 API 代理，所有 `/api` 请求会转发到 `http://localhost:3000`。

确保后端服务已启动。

## 与后端对接

本前端对接的后端 API:

- `GET /api/search` - 搜索音乐
- `POST /api/music/parse` - 解析保存音乐
- `GET /api/music/list` - 获取音乐列表
- `GET /api/music/:id` - 获取音乐详情
- `DELETE /api/music/:id` - 删除音乐
- `DELETE /api/music/batch` - 批量删除
